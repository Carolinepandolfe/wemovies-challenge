import MovieCard from 'components/MovieCard'

import * as Styled from './styles'

const Catalog = () => {
	return (
		<Styled.Container>
			<MovieCard />
			<MovieCard />
			<MovieCard />
			<MovieCard />
			<MovieCard />
			<MovieCard />
		</Styled.Container>
	)
}
export default Catalog
